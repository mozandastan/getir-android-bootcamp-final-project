<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Beyaz arka plan -->
    <item android:drawable="@android:color/white"/>

    <!-- Gri renkli çizgi (1/3) -->
    <item
        android:top="0dp"
        android:bottom="0dp"
        android:left="34dp"
        android:right="0dp">
        <shape android:shape="rectangle">
            <solid android:color="#CCCCCC"/>
        </shape>
    </item>
</layer-list>